

def agregar_producto(productos):
    nombre = input("Nombre del producto: ").strip()

    if nombre == "":
        print("El nombre no puede ser vacio")
        return #es como un break en un ciclo, mata la linea del codigo
    if nombre in productos:
        print("El producto ya existe!")
        return

    stock = int(input("Ingrese stock: "))
    precio = int(input("Ingrese precio $: "))

    productos[nombre] = [stock,precio]

productos = {} #Diccionario vacio

while True:
    print("----Menu----")
    print("1. Agregar Producto")
    print("2. Mostrar Productos")
    print("3. Buscar Productos")
    print("4. Producto más caro")
    print("5. Salir")

    while True:
        try:
            op = int(input("Ingrese la opción"))
            break
        except ValueError:
            print("Debe ingresar una opcion valida (1-5), intente nuevamente")
        
    if op == 1:
        agregar_producto(productos)
        
    elif op == 2:
        #mostrar_productos(productos)
        print(productos)

    elif op == 3:
        #buscar_productos(productos)
        print(productos)

    elif op == 4:
        #producto_mas_caro(productos)
        print("más caro")
    
    elif op == 5:
        print("Fin del programa")
        break
    




